<!DOCTYPE html>
<html>
  <head>
    <title>Detoxify.app | {{error.status}} {{message}}</title>
    <meta charset="utf-8">
    <meta content="IE=edge" http-equiv="X-UA-Compatible">
    <meta content="width=device-width, initial-scale=1" name="viewport">
    <meta content="Sandoche ADITTANE & Farbod SARAF" name="author">
    <meta content="all, follow" name="robots"/>
    <link href="favicon.png" rel="shortcut icon">
    <link href="favicon.png" rel="icon" type="image/x-icon"/>
    <meta content="en_US" property="og:locale"/>
    <meta content="website" property="og:type"/>
    <meta content="favicon.png" property="og:image"/>
    <meta content="favicon.png" name="twitter:image"/>
    <link href='/stylesheets/style.css' rel='stylesheet'/> 
    <style>
      body {
        width: 100%;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
      }

      div, h1, h2 {
        margin: 0;
      }

      .emoji {
        font-size: 48px;
      }

      h1 {
        font-size: 48px;
      }

      p {
        max-width: 600px;
        margin: 16px auto;
        text-align: center;
      }

      .button {
        width: unset;
      }
    </style>
  </head>
  <body>
    <div class="emoji">🙈</div>
    <h1>{{error.status}}</h1>
    <h2>{{message}}</h2>
    {% if error.status == '404' %}
      <p>Sorry, we could not find this page, Digital Detox would be easier with only 404 errors, isn't it ? 😂</p>
    {% endif %}
    <a href="/" class="button button--light">Search for another app!</a>
  </body>
</html>


